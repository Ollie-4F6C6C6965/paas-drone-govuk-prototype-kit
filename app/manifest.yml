---
applications:
- name: $GPAAS_APP_NAME
  command:
    - apk update
    - apk add node npm
    - npm install
    - npm start
  env:
    NODE_ENV: development
    USE_AUTH: false
    USERNAME: test
    PASSWORD: test
  memory: 256M
  buildpack: alpine
