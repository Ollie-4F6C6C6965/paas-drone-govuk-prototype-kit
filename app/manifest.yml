---
applications:
- name: $GPAAS_APP_NAME
  command:
    - npm install
    - NODE_ENV=development node ./node_modules/gulp/bin/gulp generate-assets && node listen-on-port.js
  memory: 256M
  buildpack: nodejs_buildpack
