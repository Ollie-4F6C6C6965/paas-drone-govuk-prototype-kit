---
applications:
- name: $GPAAS_APP_NAME
  command:
    - npm start
  env:
    NODE_ENV: development
    USE_AUTH: false
    USERNAME: test
    PASSWORD: test
  memory: 256M
  buildpack: nodejs_buildpack
